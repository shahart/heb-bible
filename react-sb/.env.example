# Leave blank when the frontend is served by the same host as the Spring app.
VITE_API_BASE_URL=

# Used by the Vite development proxy.
VITE_API_PROXY_TARGET=http://localhost:5173

# Override only if Google OAuth is hosted at a different URL.
VITE_GOOGLE_LOGIN_URL=/oauth2/authorization/google
